# 01 — Primera EC2

## Objetivo
Crear una instancia EC2 con Terraform y comprender `data`, `resource`, AMI e `instance_type`.

## Ejecución
```bash
terraform init
terraform validate
terraform plan
terraform apply
```

Comprueba la instancia en AWS Console > EC2 > Instances.

## Limpieza
```bash
terraform destroy
```
