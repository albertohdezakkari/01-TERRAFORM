# 03 — Entender una VPC

## Objetivo
Crear una VPC y comprender qué representa el rango `10.10.0.0/16`.

## Ejecución
```bash
terraform init
terraform validate
terraform plan
terraform apply
terraform state list
```

Debe aparecer `aws_vpc.vpc_aula`.

## Limpieza
```bash
terraform destroy
```
